namespace InstructionsProcessor.FunctionApp
{
    public enum ErrorCodes
    {
        EMPTY_STREAM = 100,
        MISSING_APPLY_OPERATION,
        INVALID_INSTRUCTION,
        MULTIPLE_APPLY_OPERATION,
        DIVIDE_BY_ZERO,
    }
}
